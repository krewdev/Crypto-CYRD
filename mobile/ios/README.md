Relay Vault iOS (SwiftUI)
------------------------

This folder will contain the SwiftUI app sources (targets: app, widgets). Xcode project/workspace can be generated using Swift Package Manager in a future step.
